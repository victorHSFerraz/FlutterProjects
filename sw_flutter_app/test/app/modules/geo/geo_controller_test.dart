import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:sw_flutter_app/app/modules/geo/geo_controller.dart';
import 'package:sw_flutter_app/app/modules/geo/geo_module.dart';

void main() {
  initModule(GeoModule());
  // GeoController geo;
  //
  setUp(() {
    //     geo = GeoModule.to.get<GeoController>();
  });

  group('GeoController Test', () {
    //   test("First Test", () {
    //     expect(geo, isInstanceOf<GeoController>());
    //   });

    //   test("Set Value", () {
    //     expect(geo.value, equals(0));
    //     geo.increment();
    //     expect(geo.value, equals(1));
    //   });
  });
}
