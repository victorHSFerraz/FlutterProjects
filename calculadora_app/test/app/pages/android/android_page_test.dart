import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AndroidPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AndroidPage(title: 'Android')));
    //  final titleFinder = find.text('Android');
    //  expect(titleFinder, findsOneWidget);
  });
}
