import 'package:calculadora_app/app/app_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(AppModule());
  // AndroidController android;
  //
  setUp(() {
    //     android = AppModule.to.get<AndroidController>();
  });

  group('AndroidController Test', () {
    //   test("First Test", () {
    //     expect(android, isInstanceOf<AndroidController>());
    //   });

    //   test("Set Value", () {
    //     expect(android.value, equals(0));
    //     android.increment();
    //     expect(android.value, equals(1));
    //   });
  });
}
