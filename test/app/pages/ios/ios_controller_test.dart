import 'package:calculadora_app/app/app_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(AppModule());
  // IosController ios;
  //
  setUp(() {
    //     ios = AppModule.to.get<IosController>();
  });

  group('IosController Test', () {
    //   test("First Test", () {
    //     expect(ios, isInstanceOf<IosController>());
    //   });

    //   test("Set Value", () {
    //     expect(ios.value, equals(0));
    //     ios.increment();
    //     expect(ios.value, equals(1));
    //   });
  });
}
