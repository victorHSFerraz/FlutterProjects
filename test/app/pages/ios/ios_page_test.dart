import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('IosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(IosPage(title: 'Ios')));
    //  final titleFinder = find.text('Ios');
    //  expect(titleFinder, findsOneWidget);
  });
}
